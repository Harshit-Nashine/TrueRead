# TrueRead — Python Dependencies
# Install: pip install -r requirements.txt
#
# NOTE: This project's training pipeline runs on Google Colab (Python 3.10).
# Most of these are pre-installed in Colab — only tf2onnx and onnx need explicit install.
# For local evaluation/visualization only, install the full list below.

# ── Core ML ───────────────────────────────────────────────────────────────────
tensorflow==2.15.0          # Training, TFLite export, GPU-native augmentation
numpy>=1.23.5,<2.0          # Array ops; pinned below 2.0 for TF 2.15 compatibility

# ── ONNX Export (for Unity Sentis) ───────────────────────────────────────────
tf2onnx>=1.16.1             # TensorFlow → ONNX conversion (opset 13)
onnx>=1.15.0                # ONNX graph validation and inspection

# ── Image Processing ──────────────────────────────────────────────────────────
opencv-python>=4.8.0        # Preprocessing pipeline (grayscale, Otsu, BFS visualization)
Pillow>=9.5.0               # Image loading utilities

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0           # Training curves, confusion matrix plot
seaborn>=0.12.0             # Confusion matrix heatmap styling

# ── Evaluation ────────────────────────────────────────────────────────────────
scikit-learn>=1.3.0         # classification_report, confusion_matrix, f1_score

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0                # Progress bars during dataset preparation
